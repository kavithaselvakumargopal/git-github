<Window x:Class="ShoppingCart.View.Orders2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoppingCart.View"
        mc:Ignorable="d"
        Title="CustomerOrders"  Width="500" Height="700"
        MinWidth="400" MinHeight="250"
        Background="LightGray">

    <Window.Resources>
        <!-- Validation Error Template -->
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="ToolTip" 
                            Value="{Binding (Validation.Errors)[0].ErrorContent, 
                                           RelativeSource={RelativeSource Self}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to WPF MVVM!"
                   FontSize="18" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <StackPanel>
            <!-- Search Section -->
            <Label Content="Search Phone" />
            <!-- Search TextBox -->
            <TextBox  Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <Button Content="Search" Command="{Binding SearchOrderCommand}" CommandParameter="{Binding SearchText}"/>

            <Label Content="Enter Customer Phone" />
            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
            <Label Content="Customer Name" />
            <TextBox Text="{Binding CustomerName, Mode=OneWay, ValidatesOnDataErrors=True}" IsReadOnly="True"/>
            <Label Content="Enter Amount" />
            <TextBox Text="{Binding TotalAmount, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
            <Label Content="Enter Quantity" />
            <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
            <Label Content="Enter Product Id" />
            <TextBox Text="{Binding ProductId, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
            <DataGrid ItemsSource="{Binding Orders}" AutoGenerateColumns="True"
                CanUserAddRows="False"
      CanUserDeleteRows="False"
      AlternationCount="2"
      RowBackground="White"
      AlternatingRowBackground="LightGray"
      GridLinesVisibility="All"
      ColumnWidth="*"
      BorderBrush="Black"
      BorderThickness="1"
      HeadersVisibility="Column"
      FontSize="14"
         SelectedItem="{Binding SelectedOrder, Mode=TwoWay}"  Height="100" />
            <Button Content="Add Order" Command="{Binding AddOrderCommand}" />
            <Button Content="Remove Order" Command="{Binding RemoveOrderCommand}"/>
        </StackPanel>
    </Grid>
</Window>